{
  "name": "travoss-platform",
  "version": "1.0.0",
  "scripts": {
    "dev": "npx -y concurrently -k -p \"[{name}]\" -n \"Backend,Frontend\" -c \"blue,green\" \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "install:all": "npm install --prefix backend && npm install --prefix frontend"
  }
}
